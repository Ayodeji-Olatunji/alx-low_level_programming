#include "main.h"
/**
 * is_palindrome - checks if a string is
 * palindrome
 * @s: the string
 * Return: 0 if it is palindrome and 1
 * if otherwise.
 *
 */
int is_palindrome(char *s)
{
	int i;

	i = 0;

	if (*s == i)
		return (0);
	return (1);
}
